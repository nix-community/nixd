# RUN: python3 %S/project_config_test.py source-selection

Project configuration is opt-in and startup root selection is immutable,
precedence-ordered, exact, and has no fallback to lower-priority candidates.
